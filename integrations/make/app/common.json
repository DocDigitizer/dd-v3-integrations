{
  "connection": {
    "type": "apiKey",
    "label": "DocDigitizer API Connection",
    "parameters": [
      {
        "name": "apiKey",
        "type": "text",
        "label": "API Key",
        "required": true,
        "help": "Your DocDigitizer API key"
      },
      {
        "name": "baseUrl",
        "type": "text",
        "label": "Base URL",
        "required": false,
        "default": "https://api.docdigitizer.com/v3/docingester",
        "help": "API base URL (change only for self-hosted instances)"
      }
    ],
    "test": {
      "url": "{{ifempty(parameters.baseUrl, 'https://api.docdigitizer.com/v3/docingester')}}/",
      "method": "GET",
      "response": {
        "valid": "{{body === 'I am alive'}}"
      }
    }
  },
  "headers": {
    "X-API-Key": "{{connection.apiKey}}"
  }
}
